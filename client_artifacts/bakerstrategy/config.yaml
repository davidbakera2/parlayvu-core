client_id: bakerstrategy
display_name: "Baker Strategy Group"
teams:
  team_id: "24a09ba4-5fdb-4986-877d-960f50ade582"
  channel_id: "19:N9YY6n-CbcfeKujiDKN_JXfudMXg82Z1eP_a0QNEOhc1@thread.tacv2"
  # meeting_notes_folder and template_path use the standard defaults from
  # app/client_config.py — override here if this client needs a custom path.
cloudflare:
  # Override the bare-<client_id> default — the live Pages project for
  # bakerstrategy.com is named "bakerstrategy-site" (legacy naming from
  # before the convention was established). Preview project follows the
  # convention; it auto-creates on first variations deploy.
  production_project: "bakerstrategy-site"
  production_domain: "bakerstrategy.com"
preferences:
  pronunciation: {}
  # Holding-company / portfolio context — David engaging Nathan about Baker
  # Strategy Group business (portfolio companies, strategic moves, BD). Same
  # direct-peer voice as the ParlayVU tenant; David is the principal here.
  tone: "Direct strategic peer for holding-company business. David is the principal at Baker Strategy Group. Treat questions as portfolio strategy / business development discussion. Cite source material from 01_Source_Material/ when available."
  # Email addresses (or Bot Framework user identifiers) allowed to DM
  # Nathan-for-this-client directly. Channel posts in the bound Teams
  # channel skip this check. Empty list = no DM access allowed (fail-closed).
  authorized_contacts:
    - "david@bakerstrategy.com"
    - "david@parlayvu.ai"
