# Client site

Scaffolded from `sites/_template/`. See `../PARLAYVU_CLIENT_SITES.md` and `../RESEND_SETUP.md`.

```bash
npm ci
npm run dev
npm run pages:deploy
```

New sites: `node ../scripts/launch-client.mjs <slug> --domain … --to … --from …`
